
source AmtB-remd.conf
source [format output/AmtB-A.job0.restart500.tcl ""]
set num_runs 1000

# prevent VMD from reading umbrella.namd by trying command only NAMD has
if { ! [catch numPes] } { source ./umbrella-mod_AmtB.namd }

